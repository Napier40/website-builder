/**
 * react-dnd item-type constants.
 *
 * PALETTE_ITEM → dragged from the left sidebar (a brand-new component)
 * CANVAS_NODE  → dragged from inside the canvas (moves an existing node)
 */
export const ItemTypes = {
  PALETTE_ITEM: 'palette-item',
  CANVAS_NODE:  'canvas-node',
};
