# ─── Website Builder · Flask Backend ─────────────────────────────────────
# Runtime dependencies. Minimum versions pinned to the releases the app
# was built against; upper bounds kept loose so security patches roll in.

# Core web framework
Flask>=3.0,<4.0
Flask-SQLAlchemy>=3.1,<4.0
Flask-JWT-Extended>=4.6,<5.0
Flask-Cors>=4.0,<6.0
Flask-Limiter>=3.5,<4.0
Flask-Migrate>=4.0,<5.0

# Auth / security
bcrypt>=4.1,<5.0
email-validator>=2.1,<3.0

# HTML sanitisation for the raw-HTML escape-hatch component
bleach>=6.1,<7.0

# Billing
stripe>=7.0,<13.0

# Config
python-dotenv>=1.0,<2.0

# Prod WSGI server (not required for `flask run`, but recommended for deploy)
gunicorn>=21.0,<24.0

# ─── Dev/Test ────────────────────────────────────────────────────────────
pytest>=8.0,<9.0
pytest-flask>=1.3,<2.0
